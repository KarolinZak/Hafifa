import { styled } from "@mui/material";

export const Container = styled("div")({
  display: "flex",
  gap: "40px",
  justifyContent: "center",
  alignItems: "center",
  height: "100vh",
  width: "100vw",
});
